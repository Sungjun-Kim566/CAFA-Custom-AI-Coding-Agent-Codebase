1. Reformatted command names in `protocol.md` (93 lines changed)
    - Section headers (e.g., line 822, 923, 1030, 1293, 1579, 1738, and all other per-command sections)
        - pre: `# SET Command: Define and Update Agent Parameters`
        - post: `` # `SET`: Define and Update Agent Parameters ``
    - Master Index bullets (lines ~37–177)
        - pre: `- **COMPARE Command: Element-wise List Comparison**`
        - post: `` - **`COMPARE`: Element-wise List Comparison** ``
    - APA reference lines (one per command section, e.g., line 918)
        - pre: `Choi, J. (2025). SET Command: Define and Update Agent Parameters. ...`
        - post: `` Choi, J. (2025). `SET`: Define and Update Agent Parameters. ... ``
    - Left alone: `- Command: @COMPARE(...)@` (code-example label, not a title) and prose uses of "command"
2. Deduped `reviewer.md` §1 against `validator.md` (checklist had drifted: reviewer's copy dropped quoting/escaping entirely, dropped "system empty"/"user starts with `/`" from symbolic integrity, and omitted `MAP` from the dependency rule)
    - `reviewer.md` (line 8–14): deleted the 6-item "Final code checklist" that duplicated validator's "Parser-critical rules"
        - pre: full checklist restated (JSON validity / schema / symbolic-LLM / control-flow / dependency / UI)
        - post: single reference — "Refer `Blueprint verification checklist` and `Parser-critical rules to confirm` at `validator.md`" — validator now owns the one canonical checklist, consumed at both the verify and review stages
3. Restored check phrasing in `validator.md` Blueprint checklist
    - `validator.md` (lines 11–12)
        - pre: `Command list for reference: ../wiki/protocol.md.` / `Code/Framework Examples: ../wiki/examples/code-bank.md`
        - post: `Every planned command is listed and sourced from ../wiki/protocol.md.` / `A closest known-good example/pattern is identified from ../wiki/examples/code-bank.md (or you state why none applies).`
4. Relaxed the one-input-control rule from mandate to recommended default across 4 files (engine-verified: `readAndGo()` in `jswrapper/CAFA Agent_files/GPT-Steps.js:2511` handles a comma-separated list of controls under one Submit; every non-`CHECKBOX` control must be filled before submission proceeds; a `CHECKBOX` may be left unchecked — its emptiness check is disabled in the engine)
    - `wiki/linter.md` (Input Controls, lines 222–223)
        - pre: `Only one input control command per visible ("show": true) turn. ...`
        - post: line 222 `Default (recommended): one input control command per visible turn ...` + new line 223 stating multiple controls are engine-supported with the fill-before-Submit / `CHECKBOX`-may-be-empty details
    - `wiki/linter.md` (TTS, line 265)
        - pre: `Only one interactive command (@TTS with button, @RADIO, etc.) per visible turn.`
        - post: `Recommended: one interactive command ... per visible turn. Multiple input controls are engine-supported (see Input Controls above).`
    - `wiki/protocol.md` (all six "Rule of Uniqueness" bullets: RADIO 3686, CHECKBOX 3789, TEXT 3882, TEXTAREA 3972, SELECT 4066, TTS 4151)
        - pre: `Rule of Uniqueness (The Single-Control Mandate): Only one interactive input control ... is permitted per visible turn. Multiple controls would create ambiguity regarding the "Submit" action ...`
        - post: `Single-Control Default: One interactive input control ... per visible turn is the recommended default. The engine also supports multiple controls in one visible turn — a single "Submit" gathers all of them ...` (the now-false "ambiguity" rationale deleted; CHECKBOX entry notes it may be left unchecked)
    - `prompts/coding-agent.md` (§5, lines 104–108)
        - pre: `**One input control per visible UI turn** — only one of RADIO, CHECKBOX, TEXT, TEXTAREA, SELECT per visible turn.`
        - post: `**Input controls per visible UI turn** — default to one ... Multiple controls in one visible turn are engine-supported (a single Submit gathers all; every non-CHECKBOX control must be filled before submission proceeds).`
    - `prompts/validator.md` (UI constraints, lines 51–54)
        - pre: `Multiple input controls per visible turn allowed for token efficiency.`
        - post: same line + appended engine constraint `(one Submit gathers all; every non-CHECKBOX control must be filled before submission proceeds).`
    - Note: `wiki/examples/code-bank.md` examples still follow the one-per-turn pattern — acceptable since that remains the stated default and linter/protocol override examples
