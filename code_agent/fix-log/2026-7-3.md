1. differentiate the codes and the descriptive texts in `linter.md` to enhance context accuracy.
2. Deleted redundant contents
    - `protocol.md` (line 184 - 620): overlaps with `linter.md`
3. Restored loop-result indexing rule lost in the deletion above
    - `protocol.md` (REPEAT deep-dive, new `### 4.3 Loop-Internal Result Access: Index with @R_i@`): loop-internal `TR`/`TU`/`TS` accumulate pipe-delimited across iterations and must be indexed with `[@R_i@]` — recovered from the deleted §8.1 (originally from `fix-log/2026-06-13-loop-result-indexing.md`)
4. Fixed dead cross-reference in `index.md`
    - CAFA Protocol row: "loop-result indexing (§8.1)" → "REPEAT §4.3" (§8.1 no longer exists in `protocol.md`)
5. Repaired orphaned rule citations in `protocol.md` (numbers referred to the deleted embedded linter copy)
    - "Rule 6.1.1" (SETJ quote-free exception) and "Rule 6.2" (EVAL, LOAD, IMPORT, GSTABLE dependency-rule headings) → explicit "(Linter & Rules, Rule …)" cross-refs
    - "Rule 9.1" mismatch: GS/TEXT/TEXTAREA sections cited Rule 9.1 for PII, but linter Rule 9.1 is about reserved `|`/`@` characters — GS heading now "(Data Privacy)"; TEXT/TEXTAREA now cite Linter & Rules Rule 9.1 for reserved-character sanitization with PII handling as separate guidance