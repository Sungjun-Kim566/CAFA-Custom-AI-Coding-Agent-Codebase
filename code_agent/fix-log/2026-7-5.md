1. Reformatted command names in `protocol.md` (93 lines changed)
    - Section headers (e.g., line 822, 923, 1030, 1293, 1579, 1738, and all other per-command sections); separated code blocks from the explanatory texts for token efficiency, providing LLM clarity in context parsing and less noise in references in code generation as a result.
        - pre: `# SET Command: Define and Update Agent Parameters`
        - post: `` # `SET`: Define and Update Agent Parameters ``
    - Master Index bullets (lines ~37–177)
        - pre: `- **COMPARE Command: Element-wise List Comparison**`
        - post: `` - **`COMPARE`: Element-wise List Comparison** ``
    - APA reference lines (one per command section, e.g., line 918)
        - pre: `Choi, J. (2025). SET Command: Define and Update Agent Parameters. ...`
        - post: `` Choi, J. (2025). `SET`: Define and Update Agent Parameters. ... ``
    - Left alone: `- Command: @COMPARE(...)@` (code-example label, not a title) and prose uses of "command"
2. Deduped `reviewer.md` §1 against `validator.md` (checklist had drifted: reviewer's copy dropped quoting/escaping entirely, dropped "system empty"/"user starts with `/`" from symbolic integrity, and omitted `MAP` from the dependency rule); reduced redundancy in prompts for token efficiency.
    - `reviewer.md` (line 8–14): deleted the 6-item "Final code checklist" that duplicated validator's "Parser-critical rules"
        - pre: full checklist restated (JSON validity / schema / symbolic-LLM / control-flow / dependency / UI)
        - post: single reference — "Refer `Blueprint verification checklist` and `Parser-critical rules to confirm` at `validator.md`" — validator now owns the one canonical checklist, consumed at both the verify and review stages
3. Restored check phrasing in `validator.md` Blueprint checklist
    - `validator.md` (lines 11–12)
        - pre: `Command list for reference: ../wiki/protocol.md.` / `Code/Framework Examples: ../wiki/examples/code-bank.md`
        - post: `Every planned command is listed and sourced from ../wiki/protocol.md.` / `A closest known-good example/pattern is identified from ../wiki/examples/code-bank.md (or you state why none applies).`
4. Relaxed the one-input-control rule from mandate to recommended default across 4 files (engine-verified: `readAndGo()` in `CAFA_ENGINE/CAFA Agent_files/GPT-Steps.js:2511` handles a comma-separated list of controls under one Submit; every non-`CHECKBOX` control must be filled before submission proceeds; a `CHECKBOX` may be left unchecked — its emptiness check is disabled in the engine)
    - `wiki/linter.md` (Input Controls, lines 222–223)
        - pre: `Only one input control command per visible ("show": true) turn. ...`
        - post: line 222 `Default (recommended): one input control command per visible turn ...` + new line 223 stating multiple controls are engine-supported with the fill-before-Submit / `CHECKBOX`-may-be-empty details
    - `wiki/linter.md` (TTS, line 265)
        - pre: `Only one interactive command (@TTS with button, @RADIO, etc.) per visible turn.`
        - post: `Recommended: one interactive command ... per visible turn. Multiple input controls are engine-supported (see Input Controls above).`
    - `wiki/protocol.md` (all six "Rule of Uniqueness" bullets: RADIO 3686, CHECKBOX 3789, TEXT 3882, TEXTAREA 3972, SELECT 4066, TTS 4151)
        - pre: `Rule of Uniqueness (The Single-Control Mandate): Only one interactive input control ... is permitted per visible turn. Multiple controls would create ambiguity regarding the "Submit" action ...`
        - post: `Single-Control Default: One interactive input control ... per visible turn is the recommended default. The engine also supports multiple controls in one visible turn — a single "Submit" gathers all of them ...` (the now-false "ambiguity" rationale deleted; CHECKBOX entry notes it may be left unchecked)
    - `prompts/coding-agent.md` (§5, lines 104–108)
        - pre: `**One input control per visible UI turn** — only one of RADIO, CHECKBOX, TEXT, TEXTAREA, SELECT per visible turn.`
        - post: `**Input controls per visible UI turn** — default to one ... Multiple controls in one visible turn are engine-supported (a single Submit gathers all; every non-CHECKBOX control must be filled before submission proceeds).`
    - `prompts/validator.md` (UI constraints, lines 51–54)
        - pre: `Multiple input controls per visible turn allowed for token efficiency.`
        - post: same line + appended engine constraint `(one Submit gathers all; every non-CHECKBOX control must be filled before submission proceeds).`
    - Note: `wiki/examples/code-bank.md` examples still follow the one-per-turn pattern — acceptable since that remains the stated default and linter/protocol override examples
5. Fixed all stale paths broken by moving `code-bank.md` from `wiki/examples/` to `wiki/`
    - `tools/code_eg_router.py` (line 24)
        - pre: `CODE_BANK = REPO_ROOT / "wiki" / "examples" / "code-bank.md"` (default invocation errored: "code bank not found")
        - post: `CODE_BANK = REPO_ROOT / "wiki" / "code-bank.md"`
    - `wiki/index.md` (line 14, KB table row; line 20, retrieve/design line)
        - pre: `examples/code-bank.md`
        - post: `code-bank.md`
    - `AGENTS.md` (line 75, framework/pattern discovery; line 78, archetype line)
        - pre: `wiki/examples/` (nonexistent directory) / `wiki/examples/code-bank.md`
        - post: `wiki/code-bank.md` in both
    - `prompts/validator.md` (line 12)
        - pre: `../wiki/examples/code-bank.md`
        - post: `../wiki/code-bank.md`
6. Wired the example router (`tools/code_eg_router.py`) into the workflow
    - `AGENTS.md` (workflow table, lines 55–56): moved `tools/code_eg_router.py` from the **design** row to the **retrieve** row (archetype discovery is retrieval)
        - pre: `| **design** | ... | tools/code_eg_router.py, prompts/coding-agent.md |`
        - post: `| **retrieve** | ... | AGENTS.md → wiki/index.md, tools/code_eg_router.py |` / design row back to `prompts/coding-agent.md` only
    - `prompts/coding-agent.md` (§1 "Example codes" key, lines 22–30): rewrote as router-first retrieval
        - pre: `(Retrieve from ../wiki/examples/code-bank.md.)` + "Refer CAFA Agent Code Bank Master Index ..." note
        - post: three sub-bullets — (1) run `python3 tools/code_eg_router.py <keywords>`; (2) on NO MATCH (exit 2) use `--list` against the Master Index or state why no archetype applies; (3) do not read `code-bank.md` wholesale — load only routed entries (this delivers the per-request context reduction: catalog + 1–3 entries instead of the ~24k-line monolith)
7. Hardened `tools/code_eg_router.py`
    - Added `--validate` mode (argparse flag + handler after `--list`): checks every master-index title has a matching `# ` entry section; missing entries exit 1, un-indexed orphan sections print as warnings only
    - NO-MATCH fallback (exit 2) now prints two pointers
        - pre: `Fall back to the CAFA protocol: wiki/protocol.md`
        - post: `Fall back to the KB index for framework discovery: wiki/index.md` + `or the CAFA protocol for command syntax: wiki/protocol.md`
8. Fixed unroutable example title found by the first `--validate` run
    - `wiki/code-bank.md` (line 20, Master Index)
        - pre: `- Adaptive Rubric Grader – GARAM Evaluation with Nested RISE Loops` (en-dash; entry heading at line 3140 uses a colon, so exact-title routing could never reach this example)
        - post: `- Adaptive Rubric Grader: GARAM Evaluation with Nested RISE Loops`
    - `--validate` now passes: 52/52 master-index titles match entry sections, 0 orphans
9. Added Project overview directory in `AGENTS.md` with explanatory descriptions on each directory or file.
    - Better architectural or high-level understanding of the workflow
10. Added `projects/` directory to store User's work 
    - For each new project, the claude code will generate separate project directories under `project/` directory.
    - This separates the user works from our coding agent source files and less confusion in which the user should find their work.
    
    ```text
    project/
    ├── README.md # README file for each project
    ├── User_Project1/ # project storage directory
    |       ├── agent.json  # agent sourcecode
    |       ├── blueprint.md # app blueprint
    |       ├── code-log.md  # code generation log
    |       └── local-code-bank.md. # local code bank only used in this project
    ├── ... Project Directory expands as User creates more projects
    ```
11. Added `code_agent` folder for easy setup on user side. download `code_agent` folder only from the repo, rename the folder that the user wants it to be, and run claude code in that folder.