1. Restructured `README.md` architecture section for the current layout (`## Two-layer separation` → `## Layer separation`)
    - Diagram updated to the current architecture: added the missing `tools/` column (`code_eg_router.py` keyword-routing into `code-bank.md`) and a `projects/<agent-slug>/` working-state box (`blueprint.md` · `agent.json` · `local-code-bank.md` · `code-log.md`) feeding from all three layers; `fix-log/` and `User_import/` kept on the bottom line.
        - pre: two boxes (Workflow Prompts | Knowledge Base) over `fix-log/ · User_import/`; no `tools/`, no `projects/`
        - post: three boxes (Workflow Prompts | Tools | Knowledge Base) over the `projects/<agent-slug>/` state box
    - Stale facts corrected in the bullets:
        - pre: `**Instructions** (CLAUDE.md + prompts/)` — `CLAUDE.md` no longer exists
        - post: `**Instructions** (AGENTS.md + prompts/)`
        - Added **Working state** bullet: debugging/revision requests start from `projects/<agent-slug>/`, **not** at the KB (mirrors `AGENTS.md` → "Project workspace").
2. Fenced all bare JSON examples in `code_agent/wiki/protocol.md` (38 blocks)
    - The file had 43 JSON examples but only 5 were inside code fences; the remaining 38 sat as bare indented text (e.g., the System Parameter Chaining agent at ~line 568, `SET` golden template at ~line 850, and every per-command example after line 470).
        - pre: bare `{ ... }` blocks flowing as prose
        - post: each wrapped in ` ```json … ``` `
    - Detection used string-aware brace balancing (braces inside `"org"` string values holding escaped JSON schemas do not miscount), so nested turn objects and multi-object templates fence as one block. All 43 fences verified paired (` ```json ` opener / bare ` ``` ` closer, closed at EOF).
    - Why: unfenced JSON is ambiguous to parse during retrieval and renders as broken prose; fenced blocks are cleanly extractable and skippable.
3. Wrapped CAFA tokens in inline code throughout `protocol.md` prose and tables (per `linter.md` Rule 6.1.3 formatting conventions; ~825 lines touched)
    - `@...@` references in all forms: simple (`@TR1@`, `@R_i@`, `@UID@`), indexed (`@TSt[index]@`, `@ParamName[1:3]@`), function-style with nested args (`@JUMP(@TN(2)@)@`, `@IF("@T_i@ > 5", ...)@`), composite loop refs (`@TR@TN(-1)@@`, `@TR@TN(-1)@[@R_i@]@`), indirection (`@@Name@[index]@`), and `JP` dot-access (`@Inventory[1].tags[0]@`, `@productInfo.name@`).
    - Bare command names: the full command vocabulary (`SET`…`MERMAID`), plus `IMPORTDATA`, `P2JSON`, `TN()`/`TN(-1)`, `D()`.
    - Parameter-system / storage names incl. plurals (`AP`s, `SP`s, `JP`s, `LOG`, `DID`, and `TR`/`TU`/`TS` in Related-Commands lists) and `cond` values (`SELF`, `SHUFFLE`, `SORT`, `DSORT`, `LINK(Target)`).
    - Applied only outside code fences and outside pre-existing inline code; existing backticked text untouched. Tables gained backticks only (no new `|`), so table structure is unchanged.
    - Artifact cleanup verified by three post-passes: split spans (`` `TN`() `` → `` `TN()` ``), commands with quoted args (`` `LOAD`("`LOG`") `` → `` `LOAD("LOG")` ``), adjacent spans separated (`@SET(...)@` `@EVAL(...)@`), and a final scan confirming zero unwrapped `@...` tokens remain in prose.
    - Deliberately left unwrapped: uppercase acronyms that are not CAFA syntax (`TS` = "Turn Specifications", `ACES`, `AIG`, `DISTRACTOR`, framework names).
    - Known pre-existing issue, not fixed here: a few argument tables have cells truncated by unescaped pipes in values (e.g., `"A|` around old line 864) — broken before this pass; separate fix needed.
